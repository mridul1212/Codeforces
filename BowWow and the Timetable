#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    int ok=0;
    for(int i=1;i<s.length();++i){
        if(s[i]=='1'){
            ok=1;
        }
    }
    cout<<(s.length()+ok)/2<<endl;
    return 0;
}
