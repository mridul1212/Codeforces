#include<bits/stdc++.h>
using namespace std;
int a,b;
int main()
{
	cin>>a>>b;
	int f=0,m=0,e=0;
	for(int i=1;i<=6;i++)
	{
		if(abs(a-i)<abs(b-i))
		{
			f++;
		}
	else	if(abs(a-i)==abs(b-i))
		{
			m++;
		}
	else
		{
			e++;
		}
	}
	cout<<f<<" "<<m<<" "<<e<<endl;
	return 0;
}
