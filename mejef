#include<bits/stdc++.h>
using namespace std;
int main ()
{
int t;
long long int n,ans=0;
cin>>t;
while(t--)
{
    cin>>n;
    if ( n%2==0)
    {
        ans= n- ((n/2)+1);
    }
    else
    {
        ans = (n-1)/2;
    }
    cout<<ans<<endl;
}

    return 0;
}
